# L32 液冷样例端到端验收记录（2026-09-16）

## 结论

本轮验收结论为 **不通过生产级验收，但通过安全门禁验收**。

系统可以完成 L32 样例上传、几何分析、回转候选识别、人工轮廓确认、工序草案规划和部分 Toolpath IR 生成；对非回转零件、非精确轮廓、图纸/STEP 槽尺寸冲突、未绑定槽工序和未认证设备均能阻断。当前仍不能生成可放行的 L32 生产 NC。

## 验收环境

- API：`http://127.0.0.1:8090`
- Web：`http://127.0.0.1:3001`
- 设备实例：`codex-e2e-l32-viii`（仅用于验收，不代表现场设备）
- API 回归：`218 passed`
- Web：TypeScript/Vite 生产构建通过
- Benchmark：24/24 样例完成基础预期检查；完整制造覆盖 0/24；5 个槽需求全部保持 ambiguous

设备实例配置校验为 `valid=true`、`production_ready=false`，阻塞原因为实际刀具模块未确认且未绑定认证后的 L32 后处理器。

## 样例结果

| 样例 | Job ID | 结果 | 关键证据 |
| --- | --- | --- | --- |
| BUQD0017 | `77321d8a98194bc0933f4f1e615ba59c` | 基础外圆草案链已修复，整件仍被图模冲突阻断 | 精确内外轮廓可接受；生成 9 道工序和 OP33-G1 外槽草案；覆盖率 50%；图纸槽底 Ø1.55 与 STEP Ø1.66 冲突被 422 拒绝；未绑定时槽审核被 409 拒绝；修复后 OP20/OP30 可达性 passed；OP40 不再错误声明整轮廓验证 |
| BUQD0292 | `9910a79f6cd44812a8215b6d3e7c4dad` | 未通过复合加工闭环 | 精确内外轮廓可接受；生成 15 道工序，包括轴向孔、内槽及 5 个外槽草案；覆盖率 4.76%；高风险工序默认禁用；内槽审核在基孔精镗未审核时被阻断；已识别 thread candidate，但未形成正式螺纹工序 |
| BUQD0094 | `eb28e6efe1a040d4b204a199f99a639e` | 主纵车和前端成形 DRAFT 均通过，背面有条件候选但整件仍未覆盖 | OP20/OP30 加工 Z=-15.387419～1.15 mm，OP21-FORM/OP31-FORM 使用左手刀沿正 Z 加工 1.15～3.05 mm；四者 HTTP 200、可达性 passed、均无生产 NC；两道粗车最大过切均为 0，精车最大过切分别为 0.002025、0.023348 mm；背面区域 -35.85～-15.387419 mm 新增禁用的 OP55-BACK/OP58-BACK，保持 uncovered |
| BUQD0325 | `628d4ee952b94a3b9c31b44ad95465d3` | 反例通过 | 判定 `unsupported`，0 装夹、0 工序、无 NC，未把 L 型非回转件伪装成车削件 |

## Toolpath / NC 检查

- BUQD0017 OP10 可以生成 `DRAFT`，`nc_generated=false`，可达性 passed。
- 修复后 BUQD0017 OP20/OP30 均可生成 `DRAFT`，可达性 passed、最大超切分别为 0 和 0.042439 mm（低于 0.05 mm 验证公差）；未加工槽位保持余料 warning。
- BUQD0017 OP40 可以生成 `DRAFT`；切断不再错误执行整件最终轮廓验证，结果保持 `verification=null`。
- 所有成功返回的车削结果均保持 `release_status=DRAFT`、`nc_generated=false`。

## Web 检查

- 部署页 HTTP 200。
- 生产 bundle 包含 L32 工作台、`groove-bindings/confirm` 和 `DRAFT` 状态。
- `npm run build` 通过。
- 本轮浏览器自动化连接不可用，因此未完成点击级交互验收；该项不计为通过。

## 2026-09-16 首轮修复复验

- 已将已识别外槽从普通外圆纵向粗精车基准轮廓中桥接剥离，槽几何仍保留给专用槽工序。
- 已允许刀具入口端倒角先增径到主体外径；只有进入较小直径后再次反向增径才判定隐藏倒扣。
- 已将整轮廓验证限定到轮廓车削和已审核槽加工；端面、切断不再产生误导性的整轮廓通过/失败结论。
- 部署后使用原 BUQD0017 Job 复验：OP20/OP30 HTTP 200、`release_status=DRAFT`、`nc_generated=false`、reachability passed；OP40 HTTP 200 且不再返回错误的整轮廓 failed。
- 已允许与已认可主轴共线且轴向覆盖充分的 OCCT 精确截面使用 excluded 局部圆柱来源面；该来源面仍不作为普通加工特征。BUQD0094 已由边投影恢复为精确内外截面。
- BUQD0094 已实现保守分区：陡峭前端成形区记录为 `planned_turning_regions`，隐藏背肩记录为 `unresolved_turning_regions`；覆盖率不会因局部刀路通过而误报整轮廓完成。
- BUQD0094 的前端成形区现由 OP21-FORM/OP31-FORM 单独反向粗精车：正 Z 进刀必须匹配左手刀，先在棒料外安全定位再径向切入；验证过切的分区 DRAFT 被硬阻断。修复离散仿真将窄切入误投射到肩面外侧的错误后，四道工序连续余料链回归通过；提交篡改区域边界的自动工序返回 409。前端仅为 DRAFT 草案，尚未校验真实刀片、刀杆与机床干涉。
- 前端四工序连续余料链已固化为独立 API 与工作台入口；服务端重新从认可轮廓推导主纵车/前端成形区，拒绝计划中被篡改的边界。部署后对原 BUQD0094 Job 调用，OP20/OP21-FORM 为留余量 warning，OP30/OP31-FORM 均 passed；最终分区最大过切分别为 0.002025 和 0.023348 mm，`nc_generated=false`。该链不包括孔槽、切断或背面。
- BUQD0094 背面大肩部新增 OP55-BACK 粗车和 OP58-BACK 精车候选；先在原坐标系桥接外槽，再按切断面转换背轴坐标。用该零件的认可轮廓和模拟安装 U150B 的设备配置单独验证：两道工序可达性 passed，粗车验证 warning（留余量），精车验证 passed，最大过切分别为 0 和 0.001818 mm，均无 NC。该模拟配置未绑定到实际 Job，也不代表现场模块已安装。
- 新增背面 OP55-BACK → OP58-BACK 连续余料 DRAFT：精车以粗车仿真后的材料采样为输入，校验阶段过切、材料体积不增和最终区域轮廓。合成区域样例在模拟 U150B 配置下两道工序连续通过，最终校核 passed、NC 仍为 false；工作台可查看阶段去除量和阻断检查。此结果不包括正面→接料→背面整件连续仿真。
- 使用 BUQD0094 的实际认可轮廓和计划快照、仅在内存中临时启用 OP55-BACK/OP58-BACK 并构造模拟 U150B 配置运行同一背面连续链：粗车 warning、精车 passed，最终最大过切 0.001818 mm、最大残料 0.000294 mm，`nc_generated=false`。未修改原 Job 或绑定设备，不能作为实机背面模块确认依据。
- 双通道集成诊断曾发现两个阻断：旧版 OP60 在 OP58-BACK 后重复切削，背面最终过切达到 2.383897 mm；原 OP40 的 2 mm 切断刀缝以成品最小 Z 为中心，向成品侵入 1 mm并导致背面最大过切 5.435 mm。现已在区域背面方案中移除 OP60，并将 OP40 刀中心、成品背面 Z0、保留材料边界拆分：刀中心位于成品背面外半个刀宽，完整 2 mm 刀缝落入牺牲余料。旧计划刀缝侵入仍会被硬阻断。
- 跨坐标材料转换不再把刀缝内的零宽边界采样当作实体表面，而使用成品侧单边材料极限。BUQD0094 新计划的 OP40 刀中心为 Z=-36.85 mm、成品背面/背轴 Z0 为 -35.85 mm、牺牲余料为 2 mm；刀缝侵入为 0。
- 区域工序已纳入正式整件 DRAFT 编译器。使用 BUQD0094 实际认可轮廓和仅驻留内存的模拟 U150B 配置，9 道工序 `OP10 → OP20 → OP21-FORM → OP30 → OP31-FORM → OP40 → OP50 → OP55-BACK → OP58-BACK` 编排完成，三个同步屏障齐全；连续材料状态及 OP30、OP31-FORM、OP58-BACK 最终轮廓均 passed，最大过切分别为 0.002025、0.023348、0.004055 mm，`nc_generated=false`。合成跨区刀尖扫掠反例仍会被 422 阻断。
- 已重新生成 BUQD0094 本地验收 Job 的计划：背面仅保留禁用的 OP50/OP55-BACK/OP58-BACK，区域仍为 uncovered；前端连续链通过。当前设备 `codex-e2e-l32-viii` 未声明背面刀具模块，因此实际 Job 的整件按钮和接口仍阻断；模拟 U150B 的整件通过不能作为现场模块确认依据。

## 剩余阻塞项

1. 现场确认 BUQD0094 的 2 mm 切断牺牲余料、刀缝方向、背轴 Z0、接料位置和夹持长度，并核实实机背面刀具模块、刀位、夹头/刀杆干涉与退出路径；软件层整件 DRAFT 已闭环，但实际 Job 的背面区域在完成这些确认前仍保持 uncovered。
2. 将 BUQD0292 图纸螺纹和槽尺寸结构化并绑定到唯一 STEP 特征，补齐螺纹正式工序。
3. 完成 BUQD0292 钻孔→粗镗→精镗→内槽的工程审核和连续余料验证。
4. 恢复 Web 点击级验收。
5. 获取真实 L32 变体、控制器版本、刀位表、后处理规范和黄金 NC，再进行空运行、试切及首件放行。
