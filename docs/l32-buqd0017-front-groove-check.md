# BUQD0017 前端细颈槽核查（2026-09-17）

目标 Job：`abf63caec83947d98155b064c45f5547`。本记录只分析原始 STEP 与当前计划，不改写用户工序，也不生成 NC。

## 找到的直接原因

原始 STEP 的已接受精确外轮廓包含细颈槽，槽底轴向宽度 0.900 mm、半径 0.830 mm，邻接外圆半径 1.815 mm。OP20/OP30 的纵车路径会跨过槽；独立的 OP33-G1 虽存在，但保持禁用，且默认配的 `TURN-GROOVE-2` 刀宽 2.000 mm，大于槽底宽度，无法合法下刀。因此目前的前端累计余料不是单纯的渲染问题。

## 本轮几何核查

- 新增只读接口 `/api/v1/jobs/{job_id}/l32/front-groove-geometry`：对每个候选切削带，取整个暂定刀宽下目标轮廓的最大半径，再加 0.020 mm 径向余量；不以刀心半径替代整刃宽，从而保护两侧圆角肩部。
- 新增只读接口 `/api/v1/jobs/{job_id}/l32/front-groove-sweep-check`：用原始 STEP 与同轴环形切削体做 OCC 实体布尔验证。暂定 0.250 mm 刀宽、15 个重叠切削带，在单独从 Ø5.63 × 5.8 mm 圆棒开始的试算中去除 37.005511 mm³；目标实体相交 0，目标缺失 0，剩余有效实体 107.383674 mm³。
- 这只证明窄刀方案在几何上可以无过切地清出部分细颈，尚未将 OP20/OP30、后部侧铣和背面型腔合并成一条完整实际工序链；`whole_part_material_verified=false`。

## 不可跳过的条件

0.250 mm 是几何测试宽度，不是已确认的 L32 现场刀具。当前绑定实例的切槽刀仍为 2.000 mm，且没有验证刀片刃形、有效伸出、刀杆/夹头干涉、控制器程序、图纸槽形要求。不能把该参考几何标为已可上机，也不能将目标 STEP 网格充当累计仿真结果。

下一步需核实可用窄切槽刀及其装配，再把经刀具/夹持检查的细颈槽、已有车削和后部扫掠放进同一根圆棒的顺序布尔减料中，逐道输出真实余料网格并与原始 STEP 比较。
