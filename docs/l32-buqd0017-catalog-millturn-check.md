# BUQD0017 L32 目录配置铣车验证（2026-09-16）

目标 Job：`abf63caec83947d98155b064c45f5547`，原始 STEP：`BUQD0017-ACC_E UQD02 公头阀芯.stp`。

## 设备边界

- 现有 Job 绑定的实际记录是 `l32-abf63cae` / VIII / U150B，保持不变。
- `L32(1).pdf` 的目录组合 VIII + U30B + U151B 作为只读参考配置；主轴标准 1° 定位，未假设选配连续 C 轴。
- U151B 的四个旋转刀位不等于已确认的实物刀具、夹头、避让或控制器后处理。

## 原件几何与当前结果

- 已认可局部回转轮廓只覆盖前侧。外侧非回转区域在原规划 Z `[-4.50, -1.05]` mm；回转截面仿真不能代表此区域的最终实体。
- MF-1 是 `PF-14` 的 1.55 × 3.00 mm 矩形底面，背面进入深度 2.55 mm。新几何草稿采用暂定 Ø1.0 mm 铣刀、0.3 mm 轴向分层、0.35 mm 横向步距，生成 9 层 72 个中心点移动。原 STEP 坐标保留在草稿中，未生成 NC。
- 该刀具无法形成矩形尖角；即使理想扫掠，至少留下 0.547235 mm³ 角部残料。刀具没有实物目录匹配，也没有证明夹持和干涉，所以该草稿不是整件通过。
- FreeCAD OCC 对原始 STEP 逐层刀具扫掠：MF-1 区域原始待去材料 11.857500 mm³，实际去除 11.301324 mm³，剩余 0.556176 mm³；目标实体交集为 0.000000 mm³。9 层残料逐层下降，结果在 `/api/v1/jobs/{job_id}/l32/catalog-back-pocket/MF-1/sweep-check` 只读接口可复验。这是局部型腔的三维体积计算，不是整件累计仿真。
- 精确 STEP 截面检查显示：在 X=-5.7、-5.3、-4.3、-3.8、-3.3 mm 各有两块独立的上下耳部截面；X=-2.8、-2.4 mm 合并为一个截面。X=-4.8 mm 正落在拓扑过渡上，截面边不能直接闭合，生成侧铣刀路时必须避开该退化采样点并以 CAD 面/边拓扑为准。
- 原始 STEP 中，径向 ±Y 两张目标侧面分别精确对应 PF-8/Face17、PF-11/Face20，每张侧面的外轮廓离散为 42 点。基于面内缩 Ø0.5 mm 刀具半径，生成两侧各 7 层、每层 15 条扫描、420 个移动；630 个实际进给段的 OCC 目标实体相交检查均为 0。只证明该候选侧面清铣路径没有切入目标，不证明外侧耳形已加工完成。
- 对同一根 Ø5.63 × 5.8 mm 圆棒按 PF-8 → PF-11 → MF-1 逐段三维布尔减料：原始库存 144.389185 mm³，三道几何阶段分别去除 7.198971、7.198972、11.301324 mm³，最终余料 118.689918 mm³；目标缺失 0，多余余料仍达 93.461828 mm³。每阶段保持单一有效实体。这是实际刀具扫掠的局部累计结果，但前侧车削、外轮廓清除、接料夹持尚未纳入，不能称为整件完成。
- 将上述 93.461828 mm³ 多余余料按 X 分区：后部槽叉 X[-5.8,-3.25] 为 40.773538 mm³，中部肩 X[-3.25,-2.35] 为 6.284948 mm³，前部回转段 X[-2.35,0] 为 46.403836 mm³。清料重点与前部车削约各占一半。
- 新增后部外侧清料候选：用 PF-8/PF-11 边界之外的 Ø0.5 mm 刀具中心线，从 ±Y 两侧各分 8 层清到 Y=0。最初以 0.02 mm 外加净距测试时，原 STEP 碰撞检查发现每侧 44 段接触、累计 0.221672 mm³；原因是侧平面不是整个零件的 Y 向投影。将净距提高至 0.18 mm 后，每侧 19 条扫描/层、608 个移动、456 段进给，原 STEP 相交体积均为 0。接口仍要求逐件运行 `/catalog-exterior-sweep-check`，不能仅凭二维轮廓放行。
- 五道几何阶段 `EXT-PF-8 → EXT-PF-11 → PF-8 → PF-11 → MF-1` 共用同一圆棒进行 OCC 连续布尔减料：两道外侧清料分别去除 17.890831、17.890832 mm³，两道侧面清铣分别去除约 7.19897 mm³，MF-1 再去除 2.164766 mm³（其余型腔材料已被外侧清料扫到）。目标缺失始终为 0；最终多余余料 66.816867 mm³，其中后部 X[-5.8,-2.35] 为 20.413463 mm³、前部 X[-2.35,0] 为 46.403836 mm³。当前仍没有完整成品，更不能把目标 STL 直接替代余料图像。
- 工艺覆盖新增独立的外侧非回转目标。当前 Job 的 MF-1、回转轮廓及外侧非回转轮廓均未完整覆盖；GET Job 会在响应中重新计算覆盖信息，但不改写用户计划。

## 继续做成真实整件仿真所需

1. 后部外侧清料已有不碰目标的候选，但仍保留约 20.4 mm³ 多余材料；需按三维投影边界进一步分区细化、考虑更小刀具或改变进刀方向。还要核对标准 1° 定位、刀具实物、排屑和装夹顺序。
2. 以 U151B 背面轴向刀具生成经过实际刀具、夹头和行程校核的 MF-1 路径；根据允许的角部圆角或电火花/二次工艺处理残料。
3. 用每道工序的实际扫掠体对圆棒做三维布尔减料，逐步与原 STEP 比较过切、欠切和连通性，再接入右侧 3D 视图。不能用目标 STL 直接冒充累计仿真。
4. 真实机床的 U30B/U151B 安装、刀位及控制器配置获得确认之前，不将该参考组合绑定到现有 Job，也不发布生产 NC。

验证：`python -m pytest tests -q`，231 项通过；原 Job 的 MF-1 草稿接口返回 72 个移动，侧铣接口各返回 420 个移动，外侧清料接口各返回 608 个移动。当前绑定实例没有 U30B/U151B，因此这些目录参考路径不能作为实机可执行工序。

## 2026-09-18 界面逐帧余料显示（另一个 Job）

在 Job `b93bc22c496e4f3f8232bc4d2d1d9f0d` 上，界面预生成 OP10、OP20、OP30、OP33-G1、OP34-NR-R、OP36-NR-F、OP52-P1-R、OP53-P1-F 共 8 道工序的 160 个实体显示帧。OP10/20/30/33 按回转半径逐层显示去料，后四道沿用实体分区帧；相邻工序的末帧与首帧文件完全一致。上述各段体积均单调下降，从 194.194692 mm³ 到原 STEP 体积 25.228027 mm³。

这些帧用于清楚展示工序间余料传递和逐步成形：车削粗精分配及铣削分区仍是几何近似，**不是**逐条实际刀具扫掠、机床碰撞或成品制造能力验证；不能因末帧与目标 STEP 重合就解除本文件所述的生产阻断。
